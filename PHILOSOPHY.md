# 설계 철학 (Design Philosophy)

> LIA(입법 영향 분석기)가 **왜 존재하고, 왜 이렇게 만들었는가**를 한곳에 모은 문서.
> 사용자용 개요는 [README](README.md), 개발 가이드는 [CLAUDE.md](CLAUDE.md), 개별 결정은 [decision-log](docs/adr/decision-log.md), 시점별 구조는 [ARCHITECTURE](docs/ARCHITECTURE.md).

---

## 1. 서비스 의도 — 왜 만드는가

**법은 시민에게 조용히, 그러나 확실하게 다가온다.** 이미 공포되어 **시행을 앞둔 법령**은 적용될 확률이 100%인데, 정작 그 영향을 받을 시민은 (1) 그런 법이 있는지도, (2) 자기에게 무슨 의미인지도, (3) 무엇을 해야 하는지도 모른다.

LIA는 그 간극을 메운다:

- **법 이름을 몰라도 된다.** 시민은 "전세 세입자한테 불리해지는 거 있어?" 같은 **자기 상황의 언어**로 묻고, 서비스가 해당 법령을 찾아준다.
- **곧 올 것만 다룬다.** 통과율 낮은 의안(추측)이 아니라 **공포 후 시행 대기 법령**(확실성)에 집중한다(D42).
- **개인의 언어로 답한다.** 조문이 아니라 "당신에게 무슨 영향이 있고, 언제까지 무엇을 하면 되는가".

한 줄로: **몰라서 놓치는 사람을 위한, 곧 시행될 법의 개인 영향 통역기.**

---

## 2. 핵심 신념 — 설계를 관통하는 원칙

### ① 그라운딩 — 지어내지 않는다
모든 분석 주장은 **주입된 조문 `source_id`만 인용**할 수 있고, 인용 없는 응답은 게이트에서 차단된다(D08). 법률 서비스에서 그럴듯한 환각은 **틀린 답보다 위험**하다. 근거를 댈 수 없으면 말하지 않는다.

### ② fail-closed — 확인 안 되면 분석하지 않는다
입력 해소는 4상태(`RESOLVED / AMBIGUOUS / NOT_FOUND_YET / UNVERIFIED`)이고, 신뢰 출처에서 확인되지 않으면 **거부**한다(D23). 뉴스·사용자 입력의 *내용*은 사실이 아니라 "어떤 법인가"의 **단서**로만 쓴다. **환각을 안 하는 것이 곧 정답**인 축이 있다.

### ③ 검색은 "식별", 그라운딩은 "정본 전문"
우리 RAG는 **검색된 청크를 프롬프트에 욱여넣지 않는다.** 검색(벡터)은 "어떤 법인가"를 **식별**하는 데만 쓰고, 식별되면 그 법의 **정본 전문을 정확 조회**해 근거로 넣는다(AnalysisEngine 정합화, D55). 잘린·틀린 청크가 근거로 새는 교과서 RAG의 고질병을 구조적으로 배제한다 — **위치 찾기와 그라운딩을 분리**한다.

### ④ LLM은 번역·추론에만, 실행 경로는 결정론
자연어의 자유도는 **번역 한 단계**(Query Planner의 Haiku)에서 끝난다. 번역기가 타입 DTO를 뱉는 순간 이후 경로는 고정되어 **결정성·캐시·인용 감사**가 보존된다(D37·D46). 에이전트 프레임워크를 쓰지 않는 이유가 이것이다 — 같은 질문이 매번 다른 경로를 타면 캐시도 감사도 무너진다.

### ⑤ 2계층 — 프로필 무관 사실을 선계산·캐시
법령 사실·조문 diff(Layer A)는 **프로필과 무관**하므로 오프라인에서 한 번 만들어 모든 사용자가 재사용한다. 프로필별 영향·대응(Layer B)만 온라인에서 생성한다(D07). 캐시 키는 `userId`가 아니라 **프로필 속성 해시** — 개인 추적 없이 재사용(D41).

### ⑥ 비용을 설계로 낮춘다
- **변경 조문만** 다룬다 — `조문변경여부='Y'` 플래그가 지목(주택법 137→6조문).
- **모델 티어링** — 분류·번역은 Haiku, 추론만 Opus.
- **캐시·prompt caching** — 완성 답이 아니라 context·사실을 재사용(D51).

### ⑦ 측정하지 않은 수치를 쓰지 않는다
성과 수치는 **부하 테스트로 실측한 뒤** 채운다. `single-flight` 같은 최적화도 **지표로 병목을 증명한 뒤** 적용한다(D48). speculative 최적화·근거 없는 자랑을 배제한다.

### ⑧ 정직성 — 문서와 코드가 같은 말을 한다
컴포넌트는 **구현 전 spec-first**로 계약을 못 박는다. 실측 함정은 숨기지 않고 기록한다(troubleshooting). 과장된 성과·미검증 주장을 문서에 남기지 않는다.

---

## 3. 왜 RAG인가 — 그리고 왜 "MCP로 실시간 조회"만으로 부족한가

우리 RAG는 청크를 반환하지 않는데(§2-③), 그렇다면 "분석 AI가 MCP로 법령 API를 실시간 조회"하는 것과 무엇이 다른가? 정직하게 나누면:

| 질의 유형 | RAG의 이점 |
|---|---|
| **법명을 아는 질의** ("주택법 제18조 뭐 바뀌어?") | **거의 없음.** MCP로 법명 조회하면 된다. 우리 정본 조회는 "캐시된 조회"일 뿐. |
| **최신성** (학습 데이터에 없는 시행예정 법) | **무승부.** MCP도 실시간이라 똑같이 얻는다. (순수 LLM 대비 이점이지 MCP 대비 아님.) |
| **모호·개념 질의** ("전세 세입자한테 불리한 법 있어?") | **유일하게 RAG만 작동.** |

**핵심은 세 번째다.** 법령 API는 **법명·키워드 검색만** 되고 **의미검색이 없다.** 시민은 법 이름을 모르고, "불리한 세입자"는 법조문에 그 표현으로 나오지 않는다(법률체는 "임차인·대항력·계약갱신요구권"…). MCP로 LLM이 키워드를 추측해도 **개념↔법률용어 간극**에서 실패한다. **임베딩 의미검색만** 시민의 언어를 법령에 잇는다.

여기에 우리 인덱스는 **시행예정 변경 조문만**으로 사전 스코핑되어 있고(노이즈↓), Discovery는 벡터 쿼리 1회로 **결정론·캐시·감사**가 된다 — 검색→읽기→재검색을 반복하는 에이전트형 MCP의 비결정·고비용과 대비된다.

> **결론:** RAG의 정당성은 전적으로 **"Discovery(개념 검색)가 핵심 기능이냐"** 에 달려 있다. 서비스 정의가 "법 이름을 모르는 시민에게 영향 있을 법을 찾아준다"인 한, 의미검색은 **대체 불가능한 핵심**이다. 만약 실사용이 대부분 "법명 대고 분석"이라면 벡터 인프라는 과투자이며 재검토 대상이다. — 우리는 **전자에 베팅한다.**

---

## 4. 의도적으로 하지 않는 것 (Non-goals)

- **자체 모델 학습·파인튜닝** — 추론=Opus, 임베딩=외부 API. 저작은 프롬프트·파이프라인에서 한다.
- **에이전트 프레임워크**(LangGraph 등) — 실행 경로가 설계 시점에 고정이라 에이전트가 아니다(D37).
- **검색된 청크를 그대로 생성 근거로 주입** — 그라운딩은 정본 전문으로(§2-③).
- **speculative 최적화** — 측정으로 병목을 증명하기 전엔 넣지 않는다(D48).
- **법률 자문 제공** — 우리는 자문이 아니라 **정보 통역**이다. 최종 판단·법적 조언은 하지 않는다(면책).
- **정량 인구통계** — 자기신고 프로필은 개인화 재료일 뿐 인구 대표성이 없다(D41).

---

## 5. 왜 그냥 ChatGPT·Claude에 물으면 안 되나

범용 LLM은 (1) **학습 컷오프** 이후의 시행예정 법을 모르고, (2) **인용 없이** 그럴듯하게 답하며, (3) **당신**을 모른다. LIA는 (1) 실시간 시행예정 코퍼스, (2) 정본 그라운딩·인용 게이트, (3) 자기신고 프로필로 이 셋을 메운다. — 이것이 "그냥 물어보면" 나오지 않는 답이다.

---

## 관련 문서

- [README](README.md) — 사용자용 개요 · "왜 ChatGPT에 물으면 안 되나"
- [CLAUDE.md](CLAUDE.md) — 개발 가이드 · 핵심 설계 원칙 · 패키지 규약
- [docs/adr/decision-log.md](docs/adr/decision-log.md) — 개별 결정(D번호)
- [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) — 시점별 구조 스냅샷(v0.1~)
- [docs/mvp/rag-pipeline-structure.md](docs/mvp/rag-pipeline-structure.md) — RAG 파이프라인 구조·역할

# Grafana 자동 프로비저닝 — 대시보드 파일 로더
apiVersion: 1
providers:
  - name: LIA
    folder: LIA
    type: file
    disableDeletion: false
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
      foldersFromFilesStructure: false
